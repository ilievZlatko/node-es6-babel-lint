/**
 * ! To change the linting standard go to .eslintrc and choose between:
 * * standard, airbnb, airbnb-base or google
 */

import message from './message';

const printOut = () => message;

printOut();
